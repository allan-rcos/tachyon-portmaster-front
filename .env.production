# Ambiente de produção (vite/vike build).
# Em produção o wire é FlatBuffers (import.meta.env.PROD === true).

# Browser → Nginx faz proxy de /api/* para o Rust.
PUBLIC_ENV__API_BASE_URL=/api

# Server (txiki) → Rust, loopback.
PUBLIC_ENV__API_SERVER_URL=http://127.0.0.1:8080
