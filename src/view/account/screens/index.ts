/**
 * As telas da conta do próprio usuário — uma por rota. Recebem o ViewModel construído
 * no `+Page` e o ligam aos componentes; não têm markup próprio além dessa
 * composição.
 *
 * @packageDocumentation
 */
export * from './AccountScreen';
