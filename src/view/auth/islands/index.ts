/**
 * Os interativos de login — `*.island.tsx`, componentes Solid com `createSignal` próprio.
 *
 * Estado de interface apenas: quem valida, busca e decide é o ViewModel.
 *
 * @packageDocumentation
 */
export * from './LoginForm.island';
