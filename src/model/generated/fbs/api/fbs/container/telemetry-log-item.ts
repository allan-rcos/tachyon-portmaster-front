// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-explicit-any */

import * as flatbuffers from 'flatbuffers';

import { TelemetryEvent } from '../../../api/fbs/common/telemetry-event.js';

export class TelemetryLogItem implements flatbuffers.IUnpackableObject<TelemetryLogItemT> {
  bb: flatbuffers.ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: flatbuffers.ByteBuffer): TelemetryLogItem {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsTelemetryLogItem(
    bb: flatbuffers.ByteBuffer,
    obj?: TelemetryLogItem,
  ): TelemetryLogItem {
    return (obj || new TelemetryLogItem()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getSizePrefixedRootAsTelemetryLogItem(
    bb: flatbuffers.ByteBuffer,
    obj?: TelemetryLogItem,
  ): TelemetryLogItem {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new TelemetryLogItem()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  id(): string | null;
  id(optionalEncoding: flatbuffers.Encoding): string | Uint8Array | null;
  id(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  event(): TelemetryEvent {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.readUint8(this.bb_pos + offset) : TelemetryEvent.Create;
  }

  description(): string | null;
  description(optionalEncoding: flatbuffers.Encoding): string | Uint8Array | null;
  description(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  timestamp(): string | null;
  timestamp(optionalEncoding: flatbuffers.Encoding): string | Uint8Array | null;
  timestamp(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 10);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  static startTelemetryLogItem(builder: flatbuffers.Builder) {
    builder.startObject(4);
  }

  static addId(builder: flatbuffers.Builder, idOffset: flatbuffers.Offset) {
    builder.addFieldOffset(0, idOffset, 0);
  }

  static addEvent(builder: flatbuffers.Builder, event: TelemetryEvent) {
    builder.addFieldInt8(1, event, TelemetryEvent.Create);
  }

  static addDescription(builder: flatbuffers.Builder, descriptionOffset: flatbuffers.Offset) {
    builder.addFieldOffset(2, descriptionOffset, 0);
  }

  static addTimestamp(builder: flatbuffers.Builder, timestampOffset: flatbuffers.Offset) {
    builder.addFieldOffset(3, timestampOffset, 0);
  }

  static endTelemetryLogItem(builder: flatbuffers.Builder): flatbuffers.Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createTelemetryLogItem(
    builder: flatbuffers.Builder,
    idOffset: flatbuffers.Offset,
    event: TelemetryEvent,
    descriptionOffset: flatbuffers.Offset,
    timestampOffset: flatbuffers.Offset,
  ): flatbuffers.Offset {
    TelemetryLogItem.startTelemetryLogItem(builder);
    TelemetryLogItem.addId(builder, idOffset);
    TelemetryLogItem.addEvent(builder, event);
    TelemetryLogItem.addDescription(builder, descriptionOffset);
    TelemetryLogItem.addTimestamp(builder, timestampOffset);
    return TelemetryLogItem.endTelemetryLogItem(builder);
  }

  unpack(): TelemetryLogItemT {
    return new TelemetryLogItemT(this.id(), this.event(), this.description(), this.timestamp());
  }

  unpackTo(_o: TelemetryLogItemT): void {
    _o.id = this.id();
    _o.event = this.event();
    _o.description = this.description();
    _o.timestamp = this.timestamp();
  }
}

export class TelemetryLogItemT implements flatbuffers.IGeneratedObject {
  constructor(
    public id: string | Uint8Array | null = null,
    public event: TelemetryEvent = TelemetryEvent.Create,
    public description: string | Uint8Array | null = null,
    public timestamp: string | Uint8Array | null = null,
  ) {}

  pack(builder: flatbuffers.Builder): flatbuffers.Offset {
    const id = this.id !== null ? builder.createString(this.id!) : 0;
    const description = this.description !== null ? builder.createString(this.description!) : 0;
    const timestamp = this.timestamp !== null ? builder.createString(this.timestamp!) : 0;

    return TelemetryLogItem.createTelemetryLogItem(builder, id, this.event, description, timestamp);
  }
}
