// automatically generated by the FlatBuffers compiler, do not modify

 

import * as flatbuffers from 'flatbuffers';

import { CargoManifestItem, CargoManifestItemT } from '../../../api/fbs/container/cargo-manifest-item.js';
import { ContainerResponse, ContainerResponseT } from '../../../api/fbs/container/container-response.js';
import { TelemetryLogItem, TelemetryLogItemT } from '../../../api/fbs/container/telemetry-log-item.js';


export class ContainerSummaryResponse implements flatbuffers.IUnpackableObject<ContainerSummaryResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ContainerSummaryResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsContainerSummaryResponse(bb:flatbuffers.ByteBuffer, obj?:ContainerSummaryResponse):ContainerSummaryResponse {
  return (obj || new ContainerSummaryResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsContainerSummaryResponse(bb:flatbuffers.ByteBuffer, obj?:ContainerSummaryResponse):ContainerSummaryResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ContainerSummaryResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

container(obj?:ContainerResponse):ContainerResponse|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ContainerResponse()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

manifest(index: number, obj?:CargoManifestItem):CargoManifestItem|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new CargoManifestItem()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

manifestLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

recentLogs(index: number, obj?:TelemetryLogItem):TelemetryLogItem|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new TelemetryLogItem()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

recentLogsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startContainerSummaryResponse(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addContainer(builder:flatbuffers.Builder, containerOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, containerOffset, 0);
}

static addManifest(builder:flatbuffers.Builder, manifestOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, manifestOffset, 0);
}

static createManifestVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startManifestVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRecentLogs(builder:flatbuffers.Builder, recentLogsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, recentLogsOffset, 0);
}

static createRecentLogsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRecentLogsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endContainerSummaryResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createContainerSummaryResponse(builder:flatbuffers.Builder, containerOffset:flatbuffers.Offset, manifestOffset:flatbuffers.Offset, recentLogsOffset:flatbuffers.Offset):flatbuffers.Offset {
  ContainerSummaryResponse.startContainerSummaryResponse(builder);
  ContainerSummaryResponse.addContainer(builder, containerOffset);
  ContainerSummaryResponse.addManifest(builder, manifestOffset);
  ContainerSummaryResponse.addRecentLogs(builder, recentLogsOffset);
  return ContainerSummaryResponse.endContainerSummaryResponse(builder);
}

unpack(): ContainerSummaryResponseT {
  return new ContainerSummaryResponseT(
    (this.container() !== null ? this.container()!.unpack() : null),
    this.bb!.createObjList<CargoManifestItem, CargoManifestItemT>(this.manifest.bind(this), this.manifestLength()),
    this.bb!.createObjList<TelemetryLogItem, TelemetryLogItemT>(this.recentLogs.bind(this), this.recentLogsLength())
  );
}


unpackTo(_o: ContainerSummaryResponseT): void {
  _o.container = (this.container() !== null ? this.container()!.unpack() : null);
  _o.manifest = this.bb!.createObjList<CargoManifestItem, CargoManifestItemT>(this.manifest.bind(this), this.manifestLength());
  _o.recentLogs = this.bb!.createObjList<TelemetryLogItem, TelemetryLogItemT>(this.recentLogs.bind(this), this.recentLogsLength());
}
}

export class ContainerSummaryResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public container: ContainerResponseT|null = null,
  public manifest: (CargoManifestItemT)[] = [],
  public recentLogs: (TelemetryLogItemT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const container = (this.container !== null ? this.container!.pack(builder) : 0);
  const manifest = ContainerSummaryResponse.createManifestVector(builder, builder.createObjectOffsetList(this.manifest));
  const recentLogs = ContainerSummaryResponse.createRecentLogsVector(builder, builder.createObjectOffsetList(this.recentLogs));

  return ContainerSummaryResponse.createContainerSummaryResponse(builder,
    container,
    manifest,
    recentLogs
  );
}
}
