// automatically generated by the FlatBuffers compiler, do not modify

 

import * as flatbuffers from 'flatbuffers';



export class OccupancyDivision implements flatbuffers.IUnpackableObject<OccupancyDivisionT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):OccupancyDivision {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsOccupancyDivision(bb:flatbuffers.ByteBuffer, obj?:OccupancyDivision):OccupancyDivision {
  return (obj || new OccupancyDivision()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsOccupancyDivision(bb:flatbuffers.ByteBuffer, obj?:OccupancyDivision):OccupancyDivision {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new OccupancyDivision()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

empty():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

loading():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

sealed():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

inTransit():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startOccupancyDivision(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addEmpty(builder:flatbuffers.Builder, empty:number) {
  builder.addFieldInt32(0, empty, 0);
}

static addLoading(builder:flatbuffers.Builder, loading:number) {
  builder.addFieldInt32(1, loading, 0);
}

static addSealed(builder:flatbuffers.Builder, sealed:number) {
  builder.addFieldInt32(2, sealed, 0);
}

static addInTransit(builder:flatbuffers.Builder, inTransit:number) {
  builder.addFieldInt32(3, inTransit, 0);
}

static endOccupancyDivision(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createOccupancyDivision(builder:flatbuffers.Builder, empty:number, loading:number, sealed:number, inTransit:number):flatbuffers.Offset {
  OccupancyDivision.startOccupancyDivision(builder);
  OccupancyDivision.addEmpty(builder, empty);
  OccupancyDivision.addLoading(builder, loading);
  OccupancyDivision.addSealed(builder, sealed);
  OccupancyDivision.addInTransit(builder, inTransit);
  return OccupancyDivision.endOccupancyDivision(builder);
}

unpack(): OccupancyDivisionT {
  return new OccupancyDivisionT(
    this.empty(),
    this.loading(),
    this.sealed(),
    this.inTransit()
  );
}


unpackTo(_o: OccupancyDivisionT): void {
  _o.empty = this.empty();
  _o.loading = this.loading();
  _o.sealed = this.sealed();
  _o.inTransit = this.inTransit();
}
}

export class OccupancyDivisionT implements flatbuffers.IGeneratedObject {
constructor(
  public empty: number = 0,
  public loading: number = 0,
  public sealed: number = 0,
  public inTransit: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return OccupancyDivision.createOccupancyDivision(builder,
    this.empty,
    this.loading,
    this.sealed,
    this.inTransit
  );
}
}
