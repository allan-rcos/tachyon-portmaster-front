// automatically generated by the FlatBuffers compiler, do not modify

 

import * as flatbuffers from 'flatbuffers';

import { OccupancyDivision, OccupancyDivisionT } from '../../../api/fbs/metrics/occupancy-division.js';


export class MetricsResponse implements flatbuffers.IUnpackableObject<MetricsResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MetricsResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMetricsResponse(bb:flatbuffers.ByteBuffer, obj?:MetricsResponse):MetricsResponse {
  return (obj || new MetricsResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMetricsResponse(bb:flatbuffers.ByteBuffer, obj?:MetricsResponse):MetricsResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MetricsResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

activeContainers():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

totalContainers():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

yardLoad():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

registeredProducts():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

occupancyDivision(obj?:OccupancyDivision):OccupancyDivision|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new OccupancyDivision()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startMetricsResponse(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addActiveContainers(builder:flatbuffers.Builder, activeContainers:number) {
  builder.addFieldInt32(0, activeContainers, 0);
}

static addTotalContainers(builder:flatbuffers.Builder, totalContainers:number) {
  builder.addFieldInt32(1, totalContainers, 0);
}

static addYardLoad(builder:flatbuffers.Builder, yardLoad:number) {
  builder.addFieldFloat64(2, yardLoad, 0.0);
}

static addRegisteredProducts(builder:flatbuffers.Builder, registeredProducts:number) {
  builder.addFieldInt32(3, registeredProducts, 0);
}

static addOccupancyDivision(builder:flatbuffers.Builder, occupancyDivisionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, occupancyDivisionOffset, 0);
}

static endMetricsResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): MetricsResponseT {
  return new MetricsResponseT(
    this.activeContainers(),
    this.totalContainers(),
    this.yardLoad(),
    this.registeredProducts(),
    (this.occupancyDivision() !== null ? this.occupancyDivision()!.unpack() : null)
  );
}


unpackTo(_o: MetricsResponseT): void {
  _o.activeContainers = this.activeContainers();
  _o.totalContainers = this.totalContainers();
  _o.yardLoad = this.yardLoad();
  _o.registeredProducts = this.registeredProducts();
  _o.occupancyDivision = (this.occupancyDivision() !== null ? this.occupancyDivision()!.unpack() : null);
}
}

export class MetricsResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public activeContainers: number = 0,
  public totalContainers: number = 0,
  public yardLoad: number = 0.0,
  public registeredProducts: number = 0,
  public occupancyDivision: OccupancyDivisionT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const occupancyDivision = (this.occupancyDivision !== null ? this.occupancyDivision!.pack(builder) : 0);

  MetricsResponse.startMetricsResponse(builder);
  MetricsResponse.addActiveContainers(builder, this.activeContainers);
  MetricsResponse.addTotalContainers(builder, this.totalContainers);
  MetricsResponse.addYardLoad(builder, this.yardLoad);
  MetricsResponse.addRegisteredProducts(builder, this.registeredProducts);
  MetricsResponse.addOccupancyDivision(builder, occupancyDivision);

  return MetricsResponse.endMetricsResponse(builder);
}
}
