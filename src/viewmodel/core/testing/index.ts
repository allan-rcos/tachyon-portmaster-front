/**
 * Apoio às factories de teste das features. Não é harness: é o que as
 * `*.factory.ts` de cada feature compartilham para gerar dado válido a partir do
 * schema Zod.
 *
 * @packageDocumentation
 */
export * from './factory-support';
