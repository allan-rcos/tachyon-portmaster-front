/**
 * Sessão e cálculo de permissões. Lê o que o cookie carrega e responde "este
 * usuário pode?" — a pergunta que `@viewmodel/core/page/authorize` faz antes de
 * liberar uma rota.
 *
 * @packageDocumentation
 */
export * from './session';
