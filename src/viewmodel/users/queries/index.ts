/**
 * Leitura de usuários. Cada `*.query.ts` recebe os cabeçalhos do
 * request (ou nada, no navegador), chama o Model e devolve o dado já no formato
 * que a página consome.
 *
 * @packageDocumentation
 */
export * from './get-user.query';
export * from './list-users.query';
