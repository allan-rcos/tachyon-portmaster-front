/**
 * Schemas Zod de autenticação: a validação de formulário e a coerção do
 * que chega como string. O schema é a fonte da regra de validação e das
 * mensagens de erro, que entram por parâmetro para falar o idioma da
 * requisição.
 *
 * @packageDocumentation
 */
export * from './login.schema';
