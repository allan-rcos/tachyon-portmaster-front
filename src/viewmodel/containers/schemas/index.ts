/**
 * Schemas Zod de contêineres: a validação de formulário e a coerção do
 * que chega como string. Os testes ao lado usam `zod-schema-faker`, então o
 * schema é a fonte tanto da regra quanto do dado de teste.
 *
 * @packageDocumentation
 */
export * from './container.schema';
export * from './manifest.schema';
