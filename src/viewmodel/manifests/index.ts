/**
 * Movimentação de carga — a rota /painel/manifestos.
 *
 * Reaproveita as mutations e o schema da feature de contêineres: o que esta
 * feature acrescenta é a TELA que escolhe operação, contêiner e produto num
 * lugar só, em vez de exigir entrar no detalhe de um contêiner.
 *
 * @packageDocumentation
 */
export * from './manifest-page.vm';
export * from './i18n/text-contracts';
