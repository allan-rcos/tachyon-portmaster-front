# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# gerado por comando (codegen) — regenerável no build
# (o flatc do SDK gera src/fbs-gen — COMMITADO)
# A saída do Paraglide (funções `m.*` tree-shakeable) vive em dist/paraglide,
# já coberta por `dist` acima — por ser build, e não fonte.

# API reference gerada pelo TypeDoc (regenerável: bun run docs:api)
docs/api

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# temporary files
*.tmp
tmp/*

# IntelliJ based IDEs
.idea
*.iml

# Finder (MacOS) folder config
.DS_Store

.claude